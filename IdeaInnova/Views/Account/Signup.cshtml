@* @{ *@
@*     ViewData["Title"] = "Signup"; *@
@* } *@

@* <div class="max-w-md mx-auto mt-10 p-6 bg-white shadow rounded"> *@
@*     <h2 class="text-xl font-bold mb-4">Create an Account</h2> *@
@*     <form asp-action="Signup" method="post"> *@
@*         <input type="text" name="username" placeholder="Username" class="form-control mb-2 w-full p-2 border rounded" required /> *@
@*         <input type="password" name="password" placeholder="Password" class="form-control mb-2 w-full p-2 border rounded" required /> *@
@*         <select name="role" class="form-control mb-2 w-full p-2 border rounded"> *@
@*             <option>User</option> *@
@*             <option>Admin</option> *@
@*         </select> *@
@*         <button type="submit" class="bg-blue-600 text-white py-2 px-4 rounded">Sign Up</button> *@
@*     </form> *@
@*     @if (ViewBag.Error != null) *@
@*     { *@
@*         <p class="text-red-500 mt-3">@ViewBag.Error</p> *@
@*     } *@
@* </div> *@


@{
    ViewData["Title"] = "Sign Up";
}

<div class="flex justify-center items-center min-h-screen bg-gradient-to-br from-blue-100 via-purple-100 to-pink-100">
    <div class="bg-white p-8 rounded-xl shadow-md w-full max-w-md">
        <div class="text-center mb-6">
            <h1 class="text-2xl font-bold text-purple-600 flex items-center justify-center">
                <span class="mr-2">💡</span> IdeaInnova
            </h1>
            <p class="text-gray-600 text-sm mt-1">Create your account to start innovating!</p>
        </div>

        <form asp-action="Signup" method="post" class="space-y-4">
            <div>
                <label class="text-sm font-medium text-gray-600">Username</label>
                <input name="username" placeholder="Choose a username"
                       class="w-full border border-gray-300 rounded px-3 py-2 focus:ring-2 focus:ring-purple-500" required />
            </div>

            <div>
                <label class="text-sm font-medium text-gray-600">Password</label>
                <input name="password" type="password" placeholder="Choose a secure password"
                       class="w-full border border-gray-300 rounded px-3 py-2 focus:ring-2 focus:ring-purple-500" required />
            </div>

            <div>
                <label class="text-sm font-medium text-gray-600">User Type</label>
                <select name="role"
                        class="w-full border border-gray-300 rounded px-3 py-2 focus:ring-2 focus:ring-purple-500">
                    <option value="User">User</option>
                    <option value="Admin">Admin</option>
                </select>
            </div>

            <button type="submit"
                    class="w-full bg-gradient-to-r from-purple-500 to-blue-500 text-white py-2 rounded-lg font-semibold shadow hover:scale-105 transition">
                Sign Up
            </button>
        </form>

        <p class="text-center text-sm mt-4 text-gray-600">
            Already have an account? <a href="/Account/Login" class="text-purple-600 font-medium">Log in</a>
        </p>
    </div>
</div>